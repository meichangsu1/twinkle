# 基于 TransferQueue 的 Agentic RL 总体设计

## 当前基线

`grpo_baseline.py` 的核心流程是同步 batch-blocking：

```python
expand_prompts = [p for prompt in batch for p in [prompt] * NUM_GENERATIONS]

ckpt_manager.sync_weights(merge_and_sync=False)
sampler.reset_prefix_cache()

all_trajectories = rollout(expand_prompts)
total_rewards, f1_rewards, cot_rewards = compute_rewards(all_trajectories)
advantages = GRPOAdvantage()(total_rewards, num_generations=NUM_GENERATIONS, scale="group")

for mini_batch in all_trajectories:
    ref_logps = model.forward_only(..., disable_lora=True)
    model.forward_backward(
        inputs=mini_batch,
        old_logps=old_logps,
        advantages=advantages,
        ref_logps=ref_logps,
    )
    model.clip_grad_and_step()
```

这个流程有三个问题：

- `rollout(expand_prompts)` 要等整个 expanded batch 完成后才返回，快完成的 prompt group 不能提前进入训练数据系统。
- rollout、reward、advantage、trainer 在同一个 driver loop 中串行运行，无法自然 overlap。
- 权重同步、staleness、长尾任务取消等控制逻辑没有独立抽象，难以切换同步/异步模式。

## 总体架构图

```text
                           ┌────────────────────────────────────┐
                           │        BaseRLPipeline / Controller  │
                           │  deploy roles / init TQ client      │
                           │  init StalenessManager / run loop   │
                           │  sync weights once per train_k      │
                           └──────────────────┬─────────────────┘
                                              │
        ┌─────────────────────────┬───────────┼─────────────────────────┬───────────────────────┐
        │                         │           │                         │                       │
        v                         v           v                         v                       v
┌──────────────────────┐ ┌──────────────────────┐ ┌──────────────────────┐        ┌──────────────────────┐
│ StalenessManager     │ │    RolloutWorker      │ │    RewardWorker       │        │ AdvantageWorker      │
│                      │ │  AsyncRollouter       │ │                      │        │                      │
│                      │ │                      │ │                      │        │                      │
│ computes:            │ │  uses:               │ │  uses:               │        │  uses:               │
│   rollout capacity   │ │    MultiTurnRollout  │ │    RewardFn          │        │    AdvantageFn       │
│   throttle/sleep hint│ │    vLLMSampler       │ │    TransferQueueClient│        │    TransferQueueClient│
│ from TQ metadata     │ │    TransferQueueClient││                      │        │                      │
└──────────────────────┘ └──────────┬───────────┘ └──────────┬───────────┘        └──────────┬───────────┘
                                     │                         │                               │
                                     v                         │                               │
┌────────────────────────────────────┐         │                                     │
│          AsyncRollouter             │         │                                     │
│                                    │         │                                     │
│  pending_queue                     │         │                                     │
│  active_tasks                      │         │                                     │
│  transfer_buffer                   │         │                                     │
│  max_concurrency from config       │         │                                     │
│  capacity hint from manager        │         │                                     │
│                                    │         │                                     │
│  submit / throttle / sleep         │         │                                     │
│  decided locally by:               │         │                                     │
│    capacity                        │         │                                     │
│    active_tasks                    │         │                                     │
│    pending_queue                   │         │                                     │
│    transfer_buffer                 │         │                                     │
└──────────┬─────────────────────────┘         │                                     │
           │                                    │                                     │
           v                                    │                                     │
┌────────────────────────────────────┐          │                                     │
│         MultiTurnRollout            │          │                                     │
│  existing multi-turn / tool loop     │          │                                     │
│  user env/tool hooks                 │          │                                     │
│                                    │          │                                     │
│  prompt group                       │          │                                     │
│    prompt * num_generations          │          │                                     │
│  -> vLLMSampler.sample()             │          │                                     │
│  -> tool/env interaction             │          │                                     │
│  -> trajectory group                 │          │                                     │
└──────────┬─────────────────────────┘          │                                     │
           │                                    │                                     │
           │ put_rollout_batch(train_k)          │ claim_reward_batch()                │ claim_reward_ready_groups()
           │                                    │ append_rewards()                    │ append_advantages()
           v                                    v                                     v
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│                                TransferQueueClient                                       │
│                                                                                          │
│  independent component dedicated to TransferQueue access                                  │
│                                                                                          │
│  control / metadata:                                                                      │
│    get_metadata() -> QueueMetadata / PartitionMetadata                                    │
│                                                                                          │
│  rollout path:                                                                            │
│    put_rollout_batch(partition_id=train_k)                                                │
│                                                                                          │
│  reward path:                                                                             │
│    claim_reward_batch() / append_rewards()                                                │
│                                                                                          │
│  advantage path:                                                                          │
│    claim_reward_ready_groups() / append_advantages()                                      │
│                                                                                          │
│  train path:                                                                              │
│    claim_train_batch() / build_train_batch_reader() / mark_trained()                      │
│                                                                                          │
│  lifecycle path:                                                                          │
│    clear_partition(train_k)                                                               │
└──────────────────────────────────────────────┬───────────────────────────────────────────┘
                                               │
                                               │ native API / KV API / StreamingDataLoader
                                               v
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│                                      TransferQueue                                        │
│                                                                                          │
│  partition: train_k                                                                      │
│  rows: trajectory samples                                                                │
│  metadata: field-ready / consumed / partition progress / policy_version / bytes          │
│                                                                                          │
│  rollout fields:                                                                         │
│    input_ids, labels, old_logps, messages, turns, group_id, generation_idx, loss_mask     │
│                                                                                          │
│  partial rollout fields:                                                                 │
│    partial_rollout, abort_count, offpolicy_token_count, partial_source_policy_version     │
│                                                                                          │
│  reward fields:                                                                          │
│    rewards, raw_rewards, reward_breakdown                                                 │
│                                                                                          │
│  advantage fields:                                                                       │
│    advantages, returns                                                                    │
│                                                                                          │
│  train state:                                                                            │
│    trained, dropped, train_metrics                                                        │
│                                                                                          │
│  read/write shape:                                                                       │
│    partition_id = train_k                                                                │
│    task_name    = rollout / reward / advantage / train                                   │
│    data_fields  = required fields for each consumer                                      │
└───────────────┬──────────────────────────────────────────────────────────────────────────┘
                │
                │ claim_train_batch()
                │ or StreamingDataLoader
                v
┌─────────────────────────────────────────────────────────────────────┐
│                            TrainerWorker                            │
│                                                                     │
│  TrainBatchReader                                                    │
│       - claim-based reader                                           │
│       - StreamingDataLoader-backed reader                            │
│                                                                     │
│  required fields:                                                    │
│    input_ids / labels / old_logps / advantages / loss_mask           │
│                                                                     │
│  train loop:                                                         │
│    forward_only() for ref_logps                                      │
│    forward_backward()                                                │
│    optimizer step                                                    │
│    mark_trained() through TransferQueueClient                        │
└───────────────┬─────────────────────────────────────────────────────┘
                │
                │ train_k done event
                v
┌─────────────────────────────────────────────────────────────────────┐
│                         Weight Sync Path                            │
│                                                                     │
│  train_k done                                                       │
│    -> BaseRLPipeline.sync_weights()                                  │
│    -> CheckpointEngineManager                                        │
│    -> vLLMSampler.receive_weights()                                  │
│    -> TransferQueueClient.clear_partition(train_k)                   │
└─────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────┐
│                       Staleness Control Path                         │
│                                                                     │
│  BaseRLPipeline                                                      │
│    -> TransferQueueClient.get_metadata()                             │
│    -> StalenessManager.get_rollout_capacity(metadata)                │
│    -> AsyncRollouter.update_capacity(capacity)                       │
│                                                                     │
│  AsyncRollouter decides:                                             │
│    submit new rollout task                                           │
│    throttle submit rate                                              │
│    sleep / backoff when staleness is out of range                    │
└─────────────────────────────────────────────────────────────────────┘
```

## 关键边界

- `TransferQueueClient` 是独立组件，是所有组件访问 TransferQueue 的唯一边界；rollouter、worker、pipeline 不直接调用底层 TQ API。
- `StalenessManager` 只根据 `max_staleness` 和 TransferQueue metadata 计算 rollout capacity / staleness 状态，不直接提交 rollout task，也不控制 reward、advantage、train 的消费额度或权重同步。
- `AsyncRollouter` 结合 `StalenessManager` 返回的 capacity、配置文件中的最大并发数、当前 `active_tasks`、`pending_queue` 和 `transfer_buffer` 状态，决定是否提交新的 rollout task；如果 staleness 接近上限则减缓提交速率，如果超过安全范围则 sleep/backoff。
- `RewardWorker`、`AdvantageWorker`、`TrainerWorker` 是长期运行的 TransferQueue 消费者，按各自配置的 batch size 通过 `TransferQueueClient` claim ready 数据。
- 权重同步固定为每个 rollout step / `train_k` 完成后同步一次，由 `BaseRLPipeline` 调用 `CheckpointEngineManager` 执行。
- `TransferQueue` 是数据协议中心，承载 rows、fields、metadata、partition 生命周期和消费状态。
- `AsyncRollouter` 只负责 prompt group 级 rollout 生产，不计算 reward、advantage 或 optimizer step。
